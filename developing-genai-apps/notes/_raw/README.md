# 원본 필기

수업에서 직접 적은 필기를 가공하지 않고 그대로 두는 곳이다.

`notes/*.md`의 정리본과 분리하는 이유: **필기에 없던 사실을 정리본에 섞으면
"수업에서 들은 것"과 "AI가 아는 것"을 독자가 구분할 수 없다.** 정리본에서
보강한 내용은 `[문서]` 출처 표기로, 판단은 `[해석]`으로 구분한다.

파일명은 `YYYY-MM-DD-주제.txt` 또는 `.md`로 둔다. 형식을 다듬지 않는다.
