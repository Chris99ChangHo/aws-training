*** 따로 공부하면 좋은 내용
JSON
HTTP/HTTPS → 네트워크
3Tier (Web - App - DB)
AWS 서비스/아키텍처 → aws이해

어제는 에이전트코어에 ci/cd하는 실습을 했는데 이걸 백엔드라고 한다면 오늘은 프론트엔드까지 자동화하고 나중에 아이디어만 있으면 이 구조 그대로 활용할 수 있는 템플릿 겸 포트폴리오 작성 가능

컨테이너
도커파일 → 이미지화 시켜서 → 도커빌드(aws에서 코드 빌드 사용)

IaC
서비스는 중단이 되면 안됨 → 코드로만들겠다 → 애플리케이션 배포 전에 인프라부터 코드로 배포하는 개념 → 그런데 이걸 자동화 CI/CD → MSA(마이크로서비스 아키텍처까지 세트로 함)

SAM 서버리스 애플리케이션 모델

관찰 3요소 → 요즘엔 표준으로 3요소를 다 하는 오픈텔레멘트리를 사용

Codeguru Reviewer → 코드 검토를 도구 + 검토자(사람)
ㄴ 요즘엔 그냥 에이전트 두개띄워서 작성, 검토를 에이전트가 다 함

코드빌드는 두가지만 알면됨 → 빌드환경(빌드할때 생겼다가 사라짐?), 빌드스펙(빌드할때 필요한 절차서)
중요 빌드스펙 yaml파일을 보면 4단계가 있음 install, pre-build, build, post-build가 있고 필요한것만 골라서 쓰면 됨

빌드할때 sns는 필수는 아니지만 편의를 위해서 약간 보안 모니터링 할때 하루종일 쳐다볼 수 없는거랑 비슷한듯

코드빌드할때는 로그를 확인해보면 도움이 될듯(모든 로그는 클라우드 워치가 수집하고 관리)
ㄴ 단계별로 몇개의 명령어 결과 상태 이런게 다 로그에 찍힘

코드디플로이
훅섹션 → Hooks: 형식으로 정할 수 있음
배포그룹 → EC2, ECS(EKS는 불가?), Lambda
빌드는 빌드 프로젝트? 디플로이는 애플리케이션을 생성해줘야함
업스케일링의 수직확장 → large → 2Xlarge(2배씩) → 이렇게 하려면 서비스가 중단됨
ㄴ 그래서 오토스케일링 활용 → 최소, 최대개수 설정(비용폭탄을 막기 위함) 
→ 언제 개수를 늘리고, 줄일지 설정(비용폭탄 막기 위함) → 인스턴스 유형을 설정(런치 템플릿)

자동화를 위한 설정이 중요함 그 다음은 aws서비스들이 알아서 함(읽고 실행하고 호출하고)
ㄴ 그래서 보통 aws의 코드들은 설정값을 대입하는 형식으로 이루어져있음

리비전타입(s3, github) → 소스코드 어디에 저장되어있냐

로드밸런서 → ec2가 elb와 같이 사용하는 이유
로드밸런서가 요청 받아서 서버들에 분산해주는 역할
다수의 ip주소를 알 필요없이 엔드포인트로 접근하는 역할

깃옵스? 코드를 깃으로만 하겠다? 이게 뭐야
ㄴ 쿠버네티스는 다 깃옵스라네
aws cli vs aws cloudformation
cli : 명령을 위한것
cloudformation : iac를 위한것
ㄴ 그래서 인프라 만들꺼면 cf써라

테스트환경, 프로덕션환경 → 똑같은데 필수기능 가볍게 / 편의 기능까지 이렇게로 나눌 수 있을듯?

모놀리스 vs 마이크로서비스
서비스 - DB vs 다중 서비스 - 다중 DB
ㄴ 마이크로서비스의 경우 서비스별로 구현하는 언어가 달라도 괜찮음

api만 따로 관리해주는 서비스 amazone api gateway → 보통 람다랑 많이 씀 → 로그는 클라우드 와치로
s3 - cloudfront도 보통 같이 많이씀

람다에 커스텀 세팅 두개가 새로 나왔음(설명은 다음 시간에)
람다는 클라우드워치로 로그 보내서 사용하는게 디폴트 설정임

SAM → 서버리스 애플리케이션 모델 : 서버리스 애플리케이션 구축에 사용하는 오픈소스프레임워크
cloudformation이랑 비슷한데 트랜스폼이라는게 있으면 sam?
ㄴ 게이트웨이랑 람다펑션등 여러가지를 같이 동시에 만들 수 있는게 장점 → 원래는 람다, api, db다 따로 만들어야함
ㄴ init기능이 있음 (에이전트코어 크리에이트랑 비슷함) → init하면 설정값 입력할 수 있고 입력하면 프로젝트가 만들어짐
ㄴ 워크플로우가 에이전트코어와 비슷함 (init → build → test → deploy)
ㄴ app.py가 람다함수, template.yaml이 sam템플릿파일
